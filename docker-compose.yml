services:
  wallapop-scraper:
    build: .
    container_name: wallapop-scraper
    shm_size: "2g"
    pids_limit: 512
    ports:
      - "8085:8000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./searches.json:/app/searches.json
    restart: always
